# Contributing

1. Create a virtual environment.
2. Install development dependencies with `python3 -m pip install -e .[dev]`.
3. Run `pytest` before committing.
4. Keep the solver deterministic and document CLI changes in `README.md`.
