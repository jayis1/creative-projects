# Contributing

Run `python3 -m pytest -q` before submitting changes. Keep the implementation dependency-free, add a regression test for behavioral changes, and document public API additions in `README.md`. Small, focused pull requests are easiest to review.
