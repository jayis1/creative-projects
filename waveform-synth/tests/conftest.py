"""Pytest configuration for waveform_synth tests."""

import sys
import os

# Ensure the package can be imported from the project root
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))